import BorrowRecordList from "@/components/features/borrow-record/BorrowRecordList";
import React from "react";

const BorrowRequest = () => {
  return <div>
    <BorrowRecordList/></div>
}

export default BorrowRequest;
