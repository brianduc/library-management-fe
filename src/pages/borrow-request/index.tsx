import BorrowRequestList from "@/components/features/borrow-request/BorrowRequestList";
import React from "react";

const BorrowRequest = () => {
  return <div>
    <BorrowRequestList/></div>
}

export default BorrowRequest;
